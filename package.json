{
  "name": "aiassistant",
  "private": true,
  "version": "1.0.0",
  "description": "Build tooling for the AI Assistant Craft plugin assets.",
  "scripts": {
    "build": "node resources/build.js",
    "watch": "node resources/build.js --watch"
  },
  "dependencies": {
    "chart.js": "^4.4.8"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}

